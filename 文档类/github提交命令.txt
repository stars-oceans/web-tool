	
https://github.com/stars-oceans/xxx.git
Get started by creating a new file or uploading an existing file. We recommend every repository include a README, LICENSE, and .gitignore.

…or create a new repository on the command line
echo "# xxx>> README.md


git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/stars-oceans/xxx.git
git push -u origin main


…or push an existing repository from the command line


git remote add origin https://github.com/stars-oceans/xxx.git
git branch -M main
git push -u origin main

1.git init
2.git remote add origin https://github.com/stars-oceans/xxx.git
3.git add .
//创建一个分支
4.git branch -M main
//推代码
5.git push -u origin main



如果是已有仓库
一个普通的文件
想上传仓库
0.初始化
git init
1.查看关联地址
git remote -v 
2.删除关联地址
git remote rm origin
3.重新链接想要的仓库
git remote add origin https://github.com/stars-oceans/xxx.git
4.//创建一个分支
git branch -M 名字



git pull origin 分支名   　　　// 更新本地仓库数据
//合并
git merge　要合并的分支名　    // 重新合并分支
git push origin 远程分支名　  // 提交合并后的数据

查看分支
git branch #列出**本地**所有的分支
查看分支详细信息

git branch -v #列出本地所有的分支, + hash 信息
查看分支与远程的关联
git branch -vv #列出本地所有的分支, + hash 信息 + 与远程的关联信息
git如何把master上的代码，合并到自己的分支

0.git branch -v （查看所在的分支）
1.切换到主分支 （master）

git checkout master

2.拉去远程仓库的代码

git pull

3.在切换到自己的分支

git checkout 分支名称

4.把主分支的代码merge合并到自己的分支上面

git merge master

5.使用上传命令push

git push origin 自己的分支

如果 把自己的分支 合并到 主分支

先从dev分支切换到master分支，使用checkout命令
git checkout master
 git merge 自己的分支
OK，键入这个命令后我们就成功地将本地的dev合并到了master分支上，之后再使用push命令将本地的master推送到远程仓库就可以啦
————————————————
这样就完成了，从主分支上的代码拉去到自己的分支的具体流程


温柔型方案：
通过git pull origin main先将本地库更新到与远程库一致的版本，但要注意本地库后来做的修改可能被覆盖，最好使用git fetch(不会自动合并)，
查看更新情况再有选择合并，或者先将本地库修改过的文件备份，git pull后再重新修改；
再运行git push即可成功。
## 谨慎使用：
暴力型方案：
git提供了一种强制上传的方式：git push -f ，它会忽略版本不一致等问题，强制将本地库上传的远程库，但是一定要谨慎使用，
因为-f会用本地库覆盖掉远程库，如果远程库上有重要更新，或者有其他同伴做的修改，也都会被覆盖，所以一定要在确定无严重后果的前提下使用此操作。
————————————————
版权声明：本文为CSDN博主「weixin_44118318」的原创文章，遵循CC 4.0 BY-SA版权协议，转载请附上原文出处链接及本声明。
原文链接：https://blog.csdn.net/weixin_44118318/article/details/85030461
强制提交代码：
//git push --force origin main
4.git push -f origin main
再
5.git add .

git commit -m '***'
//带 -u 意思是默认值
6.git push -u origin  main
























